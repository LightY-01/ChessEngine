# ChessEngine
A UCI-compatible C++ chess engine progressing from classical search algorithms (Minimax + Alpha-Beta pruning) to modern neural evaluations (NNUE). Includes a terminal-based Mate-in-X puzzle solver.
